version: '3'

services:
  app:
    build: .
    ports:
      - '5000:5000'
    volumes:
      - .:/app
    depends_on:
      - db

  db:
    image: postgres:latest
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=apimok
      - POSTGRES_HOST=db
